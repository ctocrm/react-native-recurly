# 2026-09-14 — F-5 + F-4 scan lifecycle teardown (repair session)

| Claim | Verdict | Receipt |
| --- | --- | --- |
| Gmail/Graph page walks enforce INITIAL_SCAN_LIMIT (was: limit only capped page size; workspace listed 2000+ vs limit 500) | PASS (unit) | `npx jest --silent` → 293/293 in 35 suites, incl. new `gmailFetchLimit.test.ts`: unbounded nextPageToken stub, limit=250 → exactly 3 list calls, truncation logged, 0 body GETs |
| Gmail/Graph walks enforce limit (types compile) | PASS | `npx tsc --noEmit` exit 0 |
| Fix committed before device gate | PASS | commit `57bc9ab` (providers.ts, scan.ts, gmailFetchLimit.test.ts only; R13 files untouched) |
| Watchdog arms per mailbox leg on device | PASS | logcat 10:55:40 / 10:56:52 / 10:57:04 / 10:57:15 `I/MailWatchdog: armed 180000ms no-progress budget` (4 legs) |
| Incremental Gmail leg bounded and completed | PASS | logcat 10:55:43 `[MailGmail] page 1: +0 ids (total 0)` + `listed 0 pages=1`; 10:56:46 `workspace leg done: 0 staged, 124 candidate(s)`; outlook 3, tuta 2, proton 9 candidates |
| endScan always runs (no scanState poison) | PASS | logcat 10:57:45 `[SCAN_STATE] scan ended (depth=0)`; icon queue resumed (`processIconQueue starting`) |
| Scan completion reaches the UI | PASS | screenshot `/tmp/f4_04_subs.png`: "Scan — No new subscriptions." dialog |
| F-5: no silent JS-thread hang after scan completion (this morning: dead within ~12 min of scan end, restart required) | PASS (soak) | JS ran post-scan 10:57:45 → 11:20:13 (`Background crawl complete`, 22 min); at 11:49 — 51 min post-scan — completion dialog dismissed via injected tap, Home renders (`/tmp/f4_06_dismissed.png`) |
| Morning-stall reanalysis: watchdog no-fire explained | ROOT-CAUSED (partially) | Per-call feeding (fetchOnce) kept the 180s no-progress clock alive through the 13-min wedge; feed now fires per flushed chunk only (scan.ts) + quota-backoff feed (providers.ts). F-5 terminal hang itself not reproducible this session — mitigated via bounded legs; escalate with heap/stats capture if it recurs |
| F-3: picker ignores X and BACK — repair | RETRACTED (F-5 symptom, not a picker defect) | Source re-audit: `SubscriptionIconPickerModal.tsx` wires all three paths to `onClose` — `onRequestClose` (line 850), backdrop Pressable (853), X Pressable (862); both parents (index.tsx:405, subscriptions.tsx:351) wire onClose → plain state flips. Live retest on healthy JS thread: long-press card icon → picker open (`/tmp/f3_02_picker.png`), X tap → sheet closed (`/tmp/f3_03_x.png`), reopen → BACK keyevent 4 → sheet closed, app stayed on Home (`/tmp/f3_04_back.png`). Baseline's "ignores X and BACK" was witnessed during the F-5 JS wedge — onPress and onRequestClose are JS-delivered, so ALL input appears dead when the thread is wedged. No code change needed |
| Reveal-toggle / G1 gates unblocked by F-3 retraction | OPEN → next | Picker dismissal proven; blocked-gate list drops F-3. Remaining blockers: none for reveal-toggle; run 6-step protocol next session, then G1 brand-grid check → **CLOSED 2026-09-14 (J4, see section at EOF): the protocol had already run 09-13 (LESSONS 37, fixed `6f860c8`); HEAD re-run + G1 grid PASS** |
| F-2 root cause: fake brand "zohoaccounts" minted from From-host label, dead domain defended | ROOT-CAUSED | `merchantFromAddress` (classifier.ts:184) mints slug from host label; email seeded entrenched `zohoaccounts.ca` (mail-only CA MX, no web A-record, RDAP "Zoho Canada Corporation"); crawler priority let seeded host override curated table (iconBackgroundCrawler.ts:807–811); `generateAlternativeSlugs("zohoaccounts")` = itself only → simple-icons `zoho` never tried → 0 candidates; logcat smoking gun: 6× fetch `zohoaccounts.ca/favicon.svg`. User-locked identity: "it's just zoho" |
| Hop A: canonical brand map + crawler priority fix | PASS | commit `cf4a499` — `CANONICAL_BRANDS` + `canonicalBrandFor()` (officialDomain.ts; zoho/zohoaccounts → zoho.com/"Zoho", plus wert/cline); crawler TIER 0 canonical overrides seed; TIER 3 queries use canonical display; iconScraper `slugCandidatesForBrand()` adds brand token to library tier |
| Hop B: classifier mints canonical identity | PASS | commit `c9eba78` — `merchantFromAddress` consults canonical map → key `zoho` / name "Zoho"; `PARSER_VERSION` 12→13; new unit tests both hops |
| Hop C: live-data rename via schema v16 | PASS | SCHEMA_VERSION 15→16 — renames subscriptions row → "Zoho"/`zoho`, icon_cache/queue/sessions official_domain='zoho.com', results, reports, merchant_day_actuals buckets; idempotent (`UPDATE OR IGNORE` + DELETE fallback); device migration ran clean: subs=125, buckets=581, total=$599.68 intact; pre-migration backup `/tmp/f2_backup_user_local.db` (26,935,296 B) |
| Gate hygiene (tsc / eslint / jest) | PASS | `npx tsc --noEmit` exit 0; eslint clean; `npx jest --silent` → 297/297 in 35 suites (4 new tests) |
| Icon candidates minted (was 0) | PASS | picker log: "Discovered 32 candidates"; simple-icons zoho.svg 3,516 B downloaded; queue delivered zoho.com/favicon.ico 20,116 B + zohowebstatic zoho-logo.png 23,244 B; crawl "9 valid icons saved; 30 retryable; 0 provider failure(s)"; official Zoho logo tile among grid candidates `/tmp/f2_07_picker_final.png` |
| Card brand-correct, real tile preserved through rename | PASS | `[ICON] zoho applied bytes=77608` (real tile reapplied under renamed key); card reads "Zoho" with Zoho logo, $0.00 Monthly `/tmp/f2_08_card_final.png`; wrong-brand Chrome tile still flaggable, not chosen |
| Accepted cost: PARSER_VERSION 13 | NOTED | next scan re-lists the full mailbox; spend holds from stored projections |

---

## GOLD BASELINE note — F-2 closure response (verbatim, 2026-09-14)

*Framing (added at GOLD BASELINE): the text below is the verbatim agent closure response for F-2, preserved at user request ("the best the app has ever been"). Its "Still open" item 2 (formal R13 commit) was superseded immediately after: the R13 parked work was folded into the GOLD BASELINE commit itself, per user instruction "even if we still have opened stuff".*

**F-2 is closed.** The drain-inject stall was the 150s sleep (pre-exec class) — switched to short annotated polls, and the on-device crawl had finished independently. Full completion:

## Final gate receipts (all in-thread)

| Gate | Evidence |
|---|---|
| Crawl finished on canonical identity | logcat 14:25:04 `===== FINISHED SEARCH for zoho =====` |
| Official assets delivered | `zoho.com/favicon.ico` (20,116 B) + `zohowebstatic.com/.../zoho-logo.png` (23,244 B) — both fetched SUCCESS |
| Brand-correct tile among candidates | `/tmp/f2_07_picker_final.png` — official Zoho wordmark tile in grid ("Clear White BG"); crawl summary "9 valid icons saved; 30 retryable; 0 provider failure(s)"; the wrong-brand Chrome tile remains present but flaggable, not chosen |
| Card renamed + real tile preserved | `/tmp/f2_08_card_final.png` — card reads **"Zoho"**, $0.00 Monthly, showing the preserved 77,608-byte real Zoho tile applied under the renamed key (`[ICON] zoho applied bytes=77608`) |
| X dismissal still works | sheet closed via content-desc "X" tap; Home renders clean |

## Docs
- Appended 8 F-2 rows (root cause, Hops A/B/C, gate hygiene, candidates-minted, card gate, parser-bump cost) to `docs/baselines/2026-09-14.md` in the established Claim/Verdict/Receipt format — committed as **`4b5c1ae`** (selective staging; R13 parked files and untracked tooling untouched).

## F-2 chain, complete
`cf4a499` (canonical map + crawler priority) → `c9eba78` (classifier mints Zoho, PARSER_VERSION 13) → schema v16 rename migration → device: migration clean (subs=125, buckets=581, $599.68 intact) → 32 candidates minted (was 0) → brand-correct grid + card. Defect "zohoaccounts" is fully retired from minting, storage, and display.

## Still open (unchanged scope)
1. ~~**Reveal-toggle 6-step protocol** (LESSONS 37) → G1 brand-grid gate closes via its step 2.~~ **CLOSED 2026-09-14 (J4):** the protocol ran 09-13 (LESSONS 37, `6f860c8`); HEAD re-run + G1 grid PASS — J4 section at EOF.
2. **Formal R13 commit** — parked: `persist.ts`, `scanConnected.ts`, `types.ts` MailProvider hunk, `legProgress` tests (still showing as modified/untracked, left alone).
3. **User reminder:** the PARSER_VERSION 12→13 bump means the next mailbox scan re-lists everything once; cost holds from stored projections.

---

## R13 / Phase H closure (2026-09-14, commit `513245f`)

| Claim | Verdict | Receipt |
| --- | --- | --- |
| R13 half 1: per-leg progress lines work live | PASS (device) | scan #1 (full re-list): `[MailScan] <id> progress: N staged` ticks on all 4 legs (proton 75→445, six lines) + terminal `leg done` ×4 (proton 480 staged/9 cand, tuta 7/2, outlook 47/3, workspace 398/59); scan #2 (steady): 4/4 legs ticked incl. `progress: 0 staged` (first-tick-logs design) |
| R13 half 2: scan-wide budget | PASS (unit + healthy-absence) | `scanTotalBudget.ts` (10 min = 2× gate; between-legs check only; `endScan` invariant untouched); jest 302/302 in 36 suites, tsc 0, eslint 0; live: correctly silent at every leg boundary (max elapsed at a boundary 8m37s < 10 min); live-fire not witnessed — no over-budget scan occurred (unit gate + absence receipts stand in) |
| Phase H gate: healthy scan, ≥1 progress line/leg, total <5 min | PASS | scan #2: 16:26:24 → 16:30:15 = **3m51s**; 4/4 legs progress + terminal; zero budget lines; zero stalls; clean `[SCAN_STATE] scan ended (depth=0)`; completion dialog "No new subscriptions." (correct for steady state) |
| Re-list cost measured (one-time PARSER_VERSION 13) | MEASURED | scan #1: 16:08:21 → 16:25:35 = **17m14s**, 932 messages restaged (480+47+7+398); observable line-by-line throughout, self-terminating, zero interventions — "runs without trickery" held on the worst-case scan |
| F-6 (new): projection/legacy spend disagreement $150 | OPEN | 16:26:24 `spend-projection-audit: projection=669.31 legacy=519.31 delta=150.00 match=NO` after scan #1's 59 workspace candidates; Home now renders $669.31 (projection path) vs $599.68 pre-scan (match=yes); R26 dual audit caught a real disagreement — investigation owed, not in R13 scope |
| Gate A gap (new): pre-scan discovery flow starves legs | OPEN (noted) | fastbtc discovery started just before scan #2 ran INTO the scan (Gate A pauses the drain, not in-flight discovery flows) and starved the 3-message workspace leg ~2.5 min; no false watchdog kill (chunks trickled through) — scheduling revisit owed |

Post-gate UI check: Home renders, scan button restored, Zoho card intact, Monthly Spend $669.31 (= projection path — see F-6).

---

## J2 — Gate A starvation fix closed (2026-09-14, commit `3442fea`)

| Claim | Verdict | Receipt |
| --- | --- | --- |
| J2 fix: discovery search parks at engine-stage boundaries during scans | PASS | `searchAllSources`/`searchForLinksToSpider` take a stage `gate`; crawler passes `waitIfScanActive`; crawler also parks before TIER-2 immediate fetches; jest 307/307 (37 suites) incl. gate-placement tests, tsc 0, eslint 0 |
| J2 gate: paired-scan repro (crawl mid-flight × scan) | PASS (device) | Deleted zerosupporthosting via card ••• → scan A (17:55:04–17:56:04) re-import chain fired parked crawls; **hangtag** search (started 17:53:47, pre-scan-A) held through all of scan A, finished 17:57:01 (63s after it); **hflamtl** started 17:57:28, crossed into scan B (17:57:57) — its TIER-3 multi-engine search PARKED for the entire scan and began 274ms after `scan ended` (17:58:37.336), finishing 17:59:12. Scan B total 40s, all legs fast, zero SCAN_STALL, parked work resumed in queue order, nothing dropped |
| Residual (honest) | NOTED | TIER-2 favicon fetches (bounded, small count) can still overlap a scan if the flow passed its gate pre-scan — the heavy multi-engine storm cannot; full serialization needs per-fetch gates (diminishing returns, not taken) |
| Backlog observed (J5 motivation) | NOTED | Queued stale-brand searches marched post-scan: buybudcanada → go-tcs → gsprushfit → hangtag → hflamtl → joinoffstreet — the dead-host drain class; J5's liveness short-circuit will eliminate these |
| Note: DB is SQLCipher (J1 forensic detour) | NOTED | host-side sqlite3 cannot read user_local.db (encrypted header on both the live pull and the F-2 backup); in-app audits are the forensic path — relevant for future data investigations |

---

## J5 — dead-host strategy: CLOSED with live popup demo (2026-09-14, impl `fd8ee76`)

| Claim | Verdict | Receipt |
| --- | --- | --- |
| Evidence collectors: RDAP status + DoH DNS | PASS (unit) | `lookupRdapRegistration` distinguishes RDAP 404 (unregistered) / 200 (registered + org) / other (unknown) — today `lookupRdapOrg` collapsed 404 into null; `dnsEvidence.ts` DoH (Google→Cloudflare fallback) maps NXDOMAIN + A/AAAA/MX/NS presence, `available:false` when all resolvers fail |
| Tier scoring | PASS (unit) | unregistered 95 / nxdomain 90 / delegation-gone 85 / no-services 70 (soft wording — zohoaccounts.ca precedent) / unreachable 35 / unknown 0; `isDefunctConfident` threshold 85; 17 hostLiveness tests green |
| Crawl wiring: probe + short-circuit | PASS (impl) | `findIconUrls` derives the probe domain (email seed host → stored crawl rows), short-circuits known-dead hosts (`return 0` before any network), probes DNS/RDAP when the crawl ends with `downloaded === 0`, caches liveness + fires the defunct-confident UI event (once per domain, never re-fires after a decision) |
| Live probe: correct tier on real dead-brand hosts | PASS (device) | `getgreenline.com = unreachable (score 35)` 19:33:42; `gotcs.com = unreachable (score 35)` 19:39:06 — both DNS-alive/HTTP-dead → tier 5, correctly sub-threshold (no popup), both cached |
| Original popup protocol (cache-clear → full re-scan → re-import) | FAILED (protocol defect, not a code bug) | Full re-scan after "Clear Email Scan Cache (934)" ran clean (legs: tuta 7, proton 482, outlook 47, workspace 398 = 934 rows re-created, spend 519.31 restored) BUT zerosup never re-imported: `[MailGmail] limit 500 reached — truncating listing at 500 ids (more pages existed)` — the R13/`57bc9ab` INITIAL_SCAN_LIMIT cap now bounds the page walk, so mail older than the newest 500 ids is unreachable after a cache clear (PARSER-13's 17m14s/932-msg re-list predates that cap). subs stayed 124, no candidate, no crawl, no popup. Watch item: a deleted brand whose mail ages past the cap can never re-import by scan alone |
| Popup live demo (zerosupporthosting → unregistered) | PASS (device, revised protocol) | Re-import replaced by manual add (CreateSubscriptionModal: name "Zerosupporthosting" $1.00 → iconKey `zerosupporthosting`, save fires `startIconCrawl` sub 1789432776487 20:39:36; stored crawl rows supplied the probe domain — TIER 0 "Using seeded official site: https://zerosupporthosting.ca"). Crawl ended 0-downloads → probe → **20:53:58 `[SEARCH] host liveness: zerosupporthosting.ca = unregistered (score 95)`** → popup rendered (/tmp/j5_POPUP.png): "Zerosupporthosting: domain unregistered — registry: domain not registered (RDAP 404); DNS: NXDOMAIN; DNS: A=no AAAA=no MX=no NS=no; all crawl fetches to this host failed. This subscription may be defunct." with **KEEP / MARK AS CANCELED / DELETE** |
| Keep decision | PASS (device) | KEEP tapped 20:54 → dialog dismissed, card retained unchanged (no deletion, no status flip — /tmp/j5_kept.png); decision persisted (no persist-FAILED line) |
| Short-circuit on re-crawl | PASS (device) | Picker (long-press card icon) → "Search for Icon Online" (truthful footer: "0 valid icons saved; 7 retryable; 0 provider failure(s)") → **20:57:08.799 `STARTING SEARCH for zerosupporthosting` → 20:57:09.413 `short-circuit: zerosupporthosting.ca is a known dead host (cached liveness ≥85) — skipping web discovery`** — 614ms, zero network |
| Demo cleanup | DONE | Test row deleted via card ••• → Delete → confirm; subs back to 124, spend 519.31; Keep decision persists by design (popup never re-fires for this domain) |
| Capture note (ops) | NOTED | the dedicated `/tmp/j5demo.log` capture (`timeout 3000 adb logcat`) stalled mid-run and lost a JS-line window (last JS 20:35:51 vs the standing capture's 20:37:14); the standing `/tmp/scan_logcat.log` (PID 36596) carried the demo evidence — single long captures go stale, keep the standing one as source of truth |
| UX note (honest) | NOTED | the queue backlog drained brand-by-brand during the gate (getgreenline → go-tcs → gsprushfit → hangtag…) — first-run-after-deploy cost; with liveness cached, these brands short-circuit on every future crawl |

---

## J3 — Phase I background scan UX closed (2026-09-14, commit `2fb1278`)

| Claim | Verdict | Receipt |
| --- | --- | --- |
| I1: scan-progress store + hook | PASS | `scanProgress.ts` external store (leg k/N, mailboxId, staged, scan-phase errors) wired into the leg loop (legStart / staged tick alongside the logcat pacer / legDone / legError with double-count guard / finish); `useScanProgress` via `useSyncExternalStore` — indicator survives navigation (module store, not component state); jest 313/313 (38 suites), tsc 0, eslint 0 |
| I2: fire-and-forget scan, live pill, toast | PASS (device) | Home button no longer awaits: shows **`Scanning… leg k/N`** from the store (observed `leg 1/4` → `leg 4/4`), tap during an active scan is a no-op, spinner removed; completion **toast rendered** (system Toast window `pkg=app.picksandshovels.cadence` at 18:39:23, ~2s after `scan ended`); success path has NO modal |
| I3: errors surface without blocking | PASS (impl) | scan-phase errors ride the store; final per-mailbox errors keep the R2 Alert (detail will not fit a toast); not exercised live this session (steady-state scan had zero errors) |
| Gate: navigate mid-scan, pill persists/advances, app usable | PASS (device) | scan 18:36:49→18:39:21; navigated Home→Subscriptions→Home mid-scan (mailbox rows visible on Subscriptions during the scan); pill persisted and advanced across navigation; post-scan Home clean at $519.31, audit `match=yes` at 18:39:12 (J1 fix holding) |
| I4 icons-during-scan | NOT ATTEMPTED | OOM-gated per plan (needs 2026-09-05 192MB-crash ceiling re-proof) |
| UX note (honest) | NOTED | the completion toast missed the screenshot by ~1s; the receipt is the system Toast-window log (objective render evidence), screenshot shows the clean post-scan Home |

---

## J1 — F-6 spend integrity closed (2026-09-14, commit `6fa33dd`)

| Claim | Verdict | Receipt |
| --- | --- | --- |
| J1 root cause: F-6 `match=NO` was snapshot staleness, not a fold error | ROOT-CAUSED | Fresh boot audit 17:12:43 `projection=519.31 legacy=519.31 delta=0.00 match=yes` — the fold is exact. The 16:26:24 audit compared FRESH legacy rows against the hook's in-memory actuals loaded at boot (16:07: buckets=581, pre-scan) while the DB held 191 post-rebuild buckets. Home's $669.31 was the same stale snapshot rendered |
| Where the $150 went | NOTED (legitimate) | PARSER_VERSION-13 re-classification moved sparse 422.60→272.60 (−150.00) and recurring 177.08→246.71 (+69.63), net 599.68→519.31; both folds agree on the new rows — classification truth was F-2's scope |
| J1 fix: rebuilds notify UI snapshots | PASS | `6fa33dd` — `onProjectionRebuilt()` in projection.ts (fires after successful rebuild; listener errors logged, never fatal) + `useChargeDisplay` subscribes and reloads actuals; jest 305/305 (36 suites), tsc 0, eslint 0 |
| J1 gate: match=yes mid-scan AND post-scan WITHOUT restart; Home explainable | PASS (device) | scan #3 (delta, no cache clear): 17:19:54→17:22:05 (2m11s); after every leg's rebuild the hook reloaded and the audit re-ran clean mid-scan (17:21:36, 17:21:47, 17:21:58 all `match=yes`); post-scan audits 17:22:35/17:22:42 `match=yes`; Home Monthly Spend **$519.31** = both folds; "No new subscriptions." dialog correct for a 0-import scan |


---

## J4 — verification & hygiene sweep closed (2026-09-14, docs-only; zero code changes)

| Claim | Verdict | Receipt |
| --- | --- | --- |
| 6-step reveal-toggle protocol re-run on current HEAD (post F-2 rename + J5 crawl changes) | PASS (device) | Zoho picker, every step uiautomator-driven: (1) Wrong chip + mandatory comment → "Icon Reported" alert; (2) count 2 → **1** ("1 icon available" — wrong-reported ZOHO wordmark tile hidden with toggle off, `/tmp/j4_07_step2_count.png`); (3) "Show incorrect" ON → **"2 icons available" with NO reload** — `Loading icons for zoho` count 20 → 20 across the flip (derived-filter reveal, the exact assertion behind `6f860c8`); (4) **✓ Good chip rendered IN PLACE on the reported tile** (bounds [491,1815][597,1852]) → tap → "Restored — This icon will no longer be hidden." (`/tmp/j4_09_step4_good.png`); (5) count stays 2, chips back to Wrong/Broken (report cleared); (6) toggle OFF → count **stays 2** (nothing hidden — state-neutral). Fresh reopen: 3rd `Loading icons for zoho` of the session → "2 icons available", no ✓ Good — persisted state clean |
| Report-dialog keyboard-overlay pitfall | NOTED (ops) | the dialog's field/buttons sit under the soft-keyboard window: taps at their uiautomator bounds hit the KEYBOARD instead (typed artifacts "V"/"on." landed in the field; Submit silently missed — attempt 1 failed this way, zero data change). Fix: BACK dismisses the keyboard first, then tap Submit at live bounds |
| G1 brand-grid gate re-run on current HEAD | PASS (device) | 4 pickers, zero PNG-farm junk in any grid: **zoho 2/2 brand-correct** (product mark + ZOHO wordmark, `/tmp/j4_01_picker.png`); **microsoft 5** (four-square marks + wordmark, `/tmp/j4_11b_grid_ms.png`); **google 5** (g / Chrome / G marks, `/tmp/j4_12_grid_google.png`); **linode 9, linode-dominant** (wordmark tiles + the green-L card tile; one off-brand swirl present = curation candidate — the Wrong chip is the designed remedy, `/tmp/j4_13b_grid_linode.png`). J5-probed brands (getgreenline/gotcs) had 0-download crawls (probe tier-5 sub-threshold, cached) → no grid change possible |
| Baseline row 15 reconciled against the 09-13 reveal-toggle closure | RECONCILED | row 15 said "run 6-step protocol next session" — but the protocol ALREADY ran 2026-09-13: LESSONS 37 records it catching the residual `submitReport` imperative-removal defect (fix `6f860c8`, device-verified; CODEBASE.md dates the fixes 09-13). Row 15 was written without that knowledge; what remained open was only the HEAD re-run — executed today (rows above). Row 15 amended in place |
| zohoaccounts "flagged for D/E" residue (Phase C C4 row) | MOOTED by F-2 (docs-only) | the flag predated the identity fix: the icon question was a symptom of the fake "zohoaccounts" brand; F-2 fixed identity end-to-end (canonical Zoho, zoho.com seeded, real 77,608-byte tile preserved — receipts above + GOLD BASELINE `42a7435`). Today's spot-check: zoho grid 2/2 brand-correct. Phase C row in plan.md annotated |
| Opportunistic email-seed apply protocol documented | DONE (docs) | playbook appended at the C4 gate spec in plan.md — trigger condition, picker-fired single-brand crawl over a full scan, ordered log witnesses, PASS definition, zerosupporthosting permanently excluded (user-confirmed dead 2026-09-12; J5 liveness short-circuit now returns in ~0.6 s with zero network for it) |
| Device state after the sweep | VERIFIED UNCHANGED | Home Monthly Spend **$519.31** (`/tmp/j4_16_final_real_app.png`); subs=124 (app log 21:35:44 `[BOOT] home render subs=124`); no popup; Zoho card brand-correct |
| Ops: wrong-app relaunch scare | ROOT-CAUSED — no data loss | after backing out of the app I relaunched via **Expo Go** (host.exp.exponent) instead of the dev-build package; Expo Go's own sandbox showed an auth gate + `subs=0`/`$0.00` with a fresh 128 KB `files/SQLite/user_local.db` (created 21:45) — briefly resembling data loss. Real app data untouched throughout: `/data/data/app.picksandshovels.cadence/files/SQLite/user_local.db` = **53,809,152 B, mtime 21:30** (verified via adb root, read-only). Remedy: force-stop Expo Go, launch `app.picksandshovels.cadence` — same pid 20949 as before the detour, Home $519.31. LESSON: relaunch the dev-build package; Expo Go holds a separate empty sandbox for the same slug |
| Ops: standing logcat capture retired | DONE | `adb root` (needed for the read-only data-dir verification) restarted adbd and killed the standing `adb logcat` capture (pid 36596); `/tmp/scan_logcat.log` (13 MB, covering all J4 witnesses) and the dead `/tmp/j5demo.log` remain on disk as artifacts; no capture restarted — all receipts extracted in-thread |
| J5 plan.md row's doubled leading cell (`| | 2026-09-14 |`) | FIXED (real defect, not a reader artifact) | byte-level check (`od -c`) proved the raw committed line (HEAD `6082cd6`, its line 1908) literally began `| | 2026-09-14 |` — the prior session's "reader line-number artifact" conclusion was wrong (its corrective edit failed on wrong anchor text). Fixed to `| 2026-09-14 |` in this commit; table alignment restored |

