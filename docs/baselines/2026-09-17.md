# Baseline — 2026-09-17

| Claim | Verdict | Receipt |
| --- | --- | --- |
| **R37: duplicate-mint root fix — scans repair, never twin** (`c062213`+`37662fd`+`89d27e3`+`2c4c11b`+`cd92456`+`6586fba`) | Root causes, layered: (1) rows with NULL payment_method ("slug::" identity) can never equal a candidate key ("slug::mailbox") → every scan minted twins; fixed with the **unbound-row claim** (exact identity first, then claim+repair a same-slug null-mailbox row; scan-born claimed rows backfill paymentMethod; different-mailbox rows never claimed). (2) The Shopifyemail ghost kept RE-MINTING because **ESP-brand names minted from every naming tier** — display-name tier ("Shopifyemail <orders@shopifyemail.com>"), freemail tier, and host-label variants (shopifyemail.co.uk / e.shopifyemail.net) that escape the ESP_HOSTS base list. Fix: `isEspBrandName` guards `titleCaseMerchant` (one choke point — every tier) and `merchantFromAddress`; PARSER v24→v26 restages. (3) SCHEMA **v20**: lingering scan-born null-mailbox twins merge into their bound row (paper-trail backfill, twin deleted; hand-entered rows protected). (4) Import choke point: `merchantKey !== "unknown"` candidates only (an "Unknown" row minted live from an unknown-keyed sparse hit and was deleted via UI). jest **473/473** (9 new), tsc 0. **Device**: boot v19 re-keyed the ghost → "Sean" (real store from source message); v26 restage scan clean (401 staged, 42 candidates); **stability scan: subs=103 unchanged, 0 Shopifyemail/Temuemail/Unknown rows**; Unknown junk row deleted via card menu (screenshots) | logcat 05:06 + 05:30 audits, boot MIGRATE lines 04:25/04:41/04:57, screenshots /tmp/r37_*.png |

| **R36: family cards — the Amazon shape, one card** (`6a97f3f`) | As sketched and approved: `merchantFamily.ts` (family map starting with amazon: [amazon, primevideo]; deliberately small — only user-confirmed families), `groupByFamily` (recurring member fronts the card, member names in the meta line, stable order), family-aware `matchCandidates`/`sparseSecondaryLine` + projection twins (the recurring card stacks the FAMILY's sparse actuals, `+$X Sparse` under a hairline divider — hidden when no family sparse activity this month); spend math per row unchanged. jest **465/465** (7 new incl. legacy/projection stacking parity: 100 + 154.95 → $254.95). **Device**: ONE card titled **"Amazon"**, meta `Amazon · Primevideo · Amazon`, **$11.99 Monthly** + "May have expired" chip (R35 composing); standalone Primevideo card **gone** (full-list sweep: 0 `Primevideo, recurring` cards); **Monthly Spend unchanged $484.75** (grouping is render-only). Note: the meta names THREE members because the device still carries the known twin Amazon rows (payment_method-mismatch mint class — root fix backlogged); the card is honest about what it groups | Amazon card dump (title/meta/price/chip), Primevideo sweep 0, Home $484.75 |

| **R35: lapsed-recurring expiry — Prime Video class** (`f440ce5`) | Root cause: the may-have-expired chip depends on `renewalDate`, which `candidateToSubscription` never sets — structurally unreachable for scanned rows. Fix (derived, never stored): `lapse.ts` (`isLapsedRecurring`: active recurring + no renewalDate + last corpus charge older than one period + 3d slack + grace); legacy walker (`lastRecurringChargeDate`, merchant-indexed) + projection twin (`projectionLastRecurringChargeDate`, day buckets) with parity tests; **lapsed rows contribute 0 to Monthly Spend in both paths**; card chip fires on corpus lapse; Active filter excludes / Expired includes lapsed rows. Chip honors the user's grace setting; spend paths use the default (documented split). jest **458/458** (11 new), tsc 0. **Device**: `Primevideo, recurring, May have expired` chip live with $11.99 Monthly still displayed (row honest, spend honest); 5+ more chips in the same view (Malwarebytes, Masaisrael, Walmart…); **Monthly Spend $561.90 → $484.75** (−$77.15 of lapsed recurring contributions now excluded) | Home dump $484.75, Primevideo a11y dump `recurring, May have expired`, recurring-view dump ×6 chips |

| **R34: cadence un-stamp — the "sparse but Monthly" class healed** (`edb6c4b`) | Root cause: `cadenceRepair` only writes non-empty cadences, so legacy default-cadence stamps could never clear — and it also passed empty-string cadences through unguarded. Fix: empty-cadence candidates leave `cadenceRepair` (repairs write confident cadences only); new `cadenceClear` gate clears a sparse row's stamp when the merchant's own corpus (≥2 messages) evidences NO cadence — scan-born rows only (hand-entered keep theirs), never recurring rows; billing no longer rides paper-trail-only patches (a backfill can't wipe a cadence). jest **447/447** (5 new). **Device (post v23 restage + repair scan, 01:26–01:36)**: Uber **$47.25 Monthly → $0.00 This month**; same heal for Hk, Intuit, Silvergoldbull, Systems, Temu, Scotiabank, Publicstorage, Nathanandsons, Nbnmail, Oneplus, Medzy, Lilly. Recurring rows keep evidenced cadence (Squarespace $11.99 Monthly; Malwarebytes/Masaisrael/Netgate "? Monthly/Yearly" — approved paid-unknown shapes). Audits: subs=102, recurring 561.90 + sparse 74.84 = **636.74 converged**. **Found + handled**: scan-era duplicate mints (Transunion ×2, Elite Ti ×2, a re-minted Shopifyemail) — pre-existing class (null/mismatched `payment_method` rows never match candidates → twins mint); **boot migrations self-healed all three** (v18 dedupe removed 2 twins, v19 re-archived the ESP twin; logcat 01:50). Root fix (slug-match when payment_method null) recorded as backlog | device walks 01:37–01:53, logcat 01:36 spend-audit + 01:50 MIGRATE lines |

| **R33: ESP orphan retirement — Shopifyemail/Temuemail ghosts gone** (`d75568c`) | User question answered first: shopifyemail.com is Shopify's bulk-sending rail (every store's order mail), and the old $1,439 Monthly was **all Shopify stores' order totals summed into one pre-R29 card** stamped by the default-cadence bug. Fix: `temuemail.com` joins `ESP_HOSTS` (PARSER v23, restages stored temuemail hits); SCHEMA **v19** migration re-keys each ESP-named row to the real store via its source message when resolvable — otherwise **archives** it (kept for audit, `status='archived'`, out of `getAllSubscriptions` + all spend/bucket guards; never hard-deleted); one-card rule honored (clash → archive). jest **442/442** (9 new: 7 migration + 2 temuemail rail), tsc 0, eslint 0. **Device**: boot migration logged `archived "Shopifyemail" ×2, "Temuemail" ×2, 0 re-keyed` (their stored rows had no resolvable source message — archive is the honest branch); Subscriptions list grep for Shopifyemail/Temuemail = **0 matches**; Home Monthly Spend **$561.90 unchanged** (ghosts were $0.00 rows) | logcat 00:58:59–00:59:00 migration lines, list dump 01:01, Home dump $561.90 |

| **R32: Settings → Clear Icon Cache "Failed to clear data" fixed** (`58d6327`) | Root cause was TWO-layered. Surface: `withTransactionAsync` absorbs interleaved crawler queries into the open transaction → wrapper ROLLBACK fails `cannot rollback - no transaction is active` (transient contention). First fix attempt used `withExclusiveTransactionAsync` — **device falsified it**: expo-sqlite's `Transaction.createAsync` opens a second native connection (`useNewConnection: true`) that does NOT inherit the SQLCipher `PRAGMA key`, so it reads ciphertext and fails `file is not a database` (3 retries, all failed). Final fix: keep `withTransactionAsync` (keyed main connection) + `runClearWithRetry` bounded retry (3 attempts, 300ms backoff) on the transient signature, with a code comment forbidding exclusive transactions on this encrypted DB. jest **433/433**, tsc 0, eslint 0. **Device (user path)**: Settings → Clear Icon Cache → confirm → count 51 → gone (0), no error dialog, no LogBox; Clear Spider / Crawl History → confirm → count 297 → gone (0), same. First attempt succeeded both times (retry stands by for the busy-window case; live-proven firing earlier at 23:36 attempts 1–2 before the exclusive-path diagnosis) | logcat 23:36 (retry firing) + 23:52–23:58 (clean clears), device dumps showing count-free buttons |

| **R31: clean-slate full run (user protocol)** — old backup demoted, caches cleared, pre-backup, full scan, verdict pass, post-backup | The user called it: `/tmp/gate3_full.cbak` was stale, and "before we declare anything right or stale we probably need to clear all the caches and do a full run". Executed: **(0)** old backup copied to durable `~/cadence-backups/gate3_full_OLD_2026-09-17.cbak` (18.8MB). **(1)** Settings clears: **Email Scan Cache 801→0** ✓ (the critical one — corpus rebuilt from scratch); Spider/Crawl cleared once but **self-heal re-populates it within minutes while icon-less rows exist** (the clears-vs-heal fight; a cold icon state is transient by design); **Icon Cache clear FAILS with a real bug** — `NativeDatabase.execAsync rejected → cannot rollback - no transaction is active` (ERR_INTERNAL_SQLITE_ERROR, twice on a fresh boot) — recorded as a backlog item. **(2)** Pre-run backup pulled: `~/cadence-backups/pre-fullrun-2026-09-17.db` (14.2MB; connections + sub rows + cleared corpus; app's own export flow landed in `cache/backups/` but the emulator share sheet has no target — pulled via run-as). **(3)** Full scan from the cleared corpus: `leg done: 401 staged, 42 candidates`, clean end (deep re-list UI could not be triggered through the flaky Settings UI — the normal scan rebuilt the newest-500 corpus; same bound that built every previous corpus). **(4) VERDICT PASS — the stale class HEALED**: Uber, Scotiabank, **Shopifyemail (was $1,439.00 Monthly)**, Silvergoldbull (was $801.28), Systems, Temu all now render honest `sparse — $0.00 This month` window actuals; the fresh corpus's cadence-less candidates flipped the fake Monthly stamps via cadenceRepair. REMAINING Monthly stamps on as-billed sparse rows (Intuit $680.92, Getgreenline $21.06, Google One $26.99, Github/Go Tcs/Hk $0.00) — candidate for the stale-price retirement rule. Audits post-run: legacy 711.58 / projection converging 636.74→711.58 (delta 74.84 = one sparse window not yet folded on both paths — mid-state, not a regression; match=yes was proven at the v22 restage). **(5)** Post-run backup: `~/cadence-backups/post-fullrun-2026-09-17.db` + journal (31.8MB, live-file copy with app idle, delete-journal mode) — the new known-good | ~/cadence-backups/ (4 artifacts), logcat 22:26–22:35, device dumps 22:36–22:47 |

| **R30 (J5b): tier-5 short-circuit + queue drain** (`7ec1762` + `5bc53dc`) | The J5 short-circuit only fired for cached liveness ≥85 (defunct); tier-5 `unreachable` hosts (registered, DNS fine, HTTP dead — score 35) re-ran FULL discovery every heal pass, and the icon QUEUE re-fetched its stored URL backlog blind — together a retry storm (`leadingedgehealthemails.com`: TIER 0.5–3 + fetch drains every pass) that starved the JS thread and made scan CTAs untappable (the R29 blocker). Fix: `isKnownUnreachableHost` (label `unreachable` + evidence age < **30 days**, user-set) short-circuits discovery; `isKnownDeadOrUnreachableHost` (one cache read) filters queue URLs by host. jest **433/433** (4 new), tsc 0. **Device**: `short-circuit: leadingedgehealthemails.com is unreachable (tier-5 cached…)` fired with **zero FETCH/DOWNLOAD lines** for the dead host; the scan CTA then worked on the first tap | logcat 20:12–20:14 short-circuit lines, 20:16 fetcherFor |
| **R29 device gate CLOSED via R30's unblock** (v22 restage, 20:16–20:25) | The v22 restage ran clean end-to-end (`leg done: 401 staged, 42 candidates` → `scan ended`, no watchdog kill). Results: the shopifyemail corpus hits **re-keyed to the real store** — new row `Elite Ti, sparse, $0.00 This month` (ESP display-name resolution live on device); the stale `Shopifyemail $1,439 Monthly` ghost is **gone** and `shopifyemail` search returns nothing for it — nothing can re-mint the ESP host as a merchant. The R28 residual legacy/projection divergence **resolved at the full restage**: `projection=636.74 legacy=636.74 match=yes` (sparse 74.84, recurring 561.90). Spot checks: Youtube sparse $0.00, Transunion free, Elite Ti sparse honest | logcat audits 20:25, device dumps 20:26–20:30 |

| **R29: ESP hosts are rails, not merchants** (`d7c0a04`, PARSER **v22**) | The user flagged `Shopifyemail $1,439.00 Monthly` as absurd — correct: `shopifyemail.com` is Shopify's EMAIL INFRASTRUCTURE (stores send orders through it), the merchant was minted from the host, and $1,439 is a one-off order total stamped "Monthly" by the old default-cadence bug (the resurrected 2026-09-14 "$1,439 phantom" figure). Fix: `ESP_HOSTS` class (shopifyemail.com + variants, sendgrid/mailgun/mandrill/sparkpost/amazonses/sendinblue/brevo) never mints — the real STORE resolves from the From display name or body purchase/order phrasing and imports **sparse** (`forceSparse`); bare ESP mail **drops** (`drop:esp-unresolved`). jest **429/429** (5 new `espMerchant.test.ts`), tsc 0. **Device gate: OPEN** — the v22 restage scan could not be triggered this session: the icon-heal loop re-crawls the unreachable `leadingedgehealthemails.com` host on every heal pass (the J5 dead-host class), and its network-retry storm starves the JS thread so scan CTAs never register. Unit path is proven; the restage + no-re-mint check is the remaining device verification. The stale `Shopifyemail $1,439` row still needs its 2-tap manual delete (nothing re-mints it after v22) | classifier.ts resolveEspSender + ESP_HOSTS; logcat 19:35–19:50 retry storm |
| **R28: one card per merchant — identity matching + dedupe migration** (`6351d21` + keeper fix `05dce46`, SCHEMA **v18**) | Rows were matched to scan candidates by DISPLAY NAME (`${s.name}::…` at scanConnected.ts:145/192), so name variants ("YouTube" product-map vs "Youtube" From-host mint; restore-era names) defeated the match and MINTED duplicate cards instead of repairing — the two-YouTube-cards report. Fix: rows match by **name-slug identity** (`nameToSlug(name)` + mailbox; icon_key is NOT identity — the default "plus" or user-picked icons drift). Schema v18 one-time dedupe: group by slug+mailbox, keep the **freshest** row (then paper-trail; known price ranks LAST — a stale twin often carries a wrong-but-known price while the honest fresh row is priceUnknown), merge missing paper-trail links, delete twins; merchant_day_actuals is merchant-keyed so twins share buckets. jest 424/424 (3 new `identityMatch.test.ts`), tsc 0. **Device**: migration ran at boot (`1 twin removed across 1 group`); the stale "YouTube $549.99 Monthly" survivor deleted once manually (proven un-resurrectable — no candidate carries $549.99); the next scan minted exactly ONE honest card (`Youtube sparse $0.00 This month`, paper-trailed). One keeper-rule correction landed after the first device run (freshest-first) for all future DBs | logcat `[MIGRATE] subscription dedupe v18`, device dumps 18:25–18:50 |
| R28 residuals (recorded) | Legacy buckets still hold restore-era folds (legacy audit 711.58 vs projection 636.74; the projection path drives Home) — they converge at the next FULL restage (parser bump/deep scan). Single stale-priced rows without twins (`Shopifyemail $1,439.00 Monthly` class) persist by design — the repair path never downgrades a stored price; future heal candidates | logcat spend-audit pairs |
| **R27: purchase-proof gate** (`cd1d290` + tuning `e75c73b`, PARSER **v21**) | The Pixel Watch 5 pre-order ad ("$549.99", "monthly" financing, YouTube social footer) minted a "YouTube sparse $549.99 Monthly" subscription (user report: never paid YouTube monthly; the email was advertising). Fix, per plan + web research (Gmail `CATEGORY_*` labelIds already in our metadata response; RFC 8058 `List-Unsubscribe` in the same `payload.headers`; SpamAssassin-style weighted evidence): **amounts require payment proof**; marketing evidence drops proof-less mail; **proof is tiered** — STRONG charge artifacts (doc number, total, card ending, `GPA.` order id) always beat marketing, GENERIC soft anchors ("payment method", bare receipt/invoice/statement words) survive only light marketing (<4; device evidence: the ad's fine-print `proof:payment-event` + marketing score 10 no longer saves it). Cadence by evidence: STRONG anchors alone (`billed monthly`, `$13.99/mo` price-adjacent, `renews yearly`, domain registration/renewal price); weak words need proof + recurring kind. **Google products distinct**: `google-store` its own product (sender local-part `googlestore-*`, `store.google.com`), Play keyed by `GPA.` ids, body word-scan demoted to URL/qualified-phrase (social-footer "YouTube" keys nothing). Provider hints are OPTIONAL weights (Gmail/Workspace labelIds + list headers; Graph `$select` subset) — heuristics stand alone (proven by hints-absent unit fixture). jest 421/421 (12 new `purchaseProof.test.ts`), tsc 0 | classifier.ts, providers.ts, types.ts |
| **R27 device gate (workspace restage, v21)** | PASS — post-restage candidate roll has **no google-store candidate and no $549.99 anywhere**; the deleted Google Store row did **not resurrect**; the stale "YouTube $549.99 Monthly" row retired (only honest `Youtube sparse $0.00 This month` remains — a real receipt, price unknown); **Google Play** rows re-keyed with real amounts (19.99 monthly, strong `gpa-order`+`doc-number`+`total-price` proof); **Medzy demoted to sparse $0.00** (its "Monthly" was a weak word — the exact class the user flagged); Transunion **free**, Uber sparse, ?-class reads window actuals. Audit `projection=636.74 legacy=636.74 delta=0.00 match=yes`. Environment fought back (host load 12, Gmail quota 403 backoffs, one legitimate watchdog SCAN_STALL, emulator reboot + PIN/locksettings churn re-locking the vault) — all recovered loud, data intact | logcat 12:23/13:07 R27DIAG candidate dumps, device UI dumps |
| Known residual (recorded, not chased) | Restore-era rows on other merchants still show as-billed stale shapes (`Shopifyemail $1,439.00 Monthly`, `Uber $47.25 Monthly` duality across renders) — pre-existing class; the v21 restage covers the connected workspace mailbox; other legs re-derive on their next scans | device dumps 13:10–13:20 |

