# 2026-09-16 — Phase O session

| Claim | Verdict | Receipt |
| --- | --- | --- |
| O1 no-icon placeholder replaces plus.png fallback | VERIFIED on device: glyph renders on icon-less rows (Rotaryengine, Google One) post-import | `d95d021`, /tmp/gate3_final_home.png |
| O2 Add-mailbox brand icons | VERIFIED on device: tuta row shows the curated brand mark, all other providers show the glyph — no fake plus anywhere | `d95d021`, /tmp/o2_picker_brands.png |
| O3 encrypted export (streaming, passphrase-keyed) | WORKS on device — file written natively, 135KB fresh / 29.5MB with data | cache/exports/cadence_encrypted_1789557895943.cbak |
| O3 encrypted import round-trip | PASS on device: pick → passphrase → probe → rekey → scan → "Import Complete" | /tmp/roundtrip.cbak, v6 dump |
| Fresh-install boot after pm clear | REGRESSION FOUND + FIXED: first key never minted behind the gate (`673e761`); device now boots to $0.00 Home | r11 dump |
| Share sheet for .cbak | FAILS on bare emulator (no handler app) — environment gap, not code | "No apps can perform this action" screenshot |
| Old-format intermediate backup recoverable | NO — header write corrupted page 1; 24 offline sqlcipher combos all hmac-fail | /tmp/gate_backup.cbak unrecoverable |
| **Pre-wipe dev dataset (136 subs, mailbox sessions)** | **LOST** — pm clear ran with only the corrupted intermediate backup in hand | this file |
| Post-wipe rebuild: both mailboxes reconnected, 53 subs / $355.79 | RESTORED on device UI, matches spend-audit (`projection=355.79 legacy=355.79 match=yes` through 14:09) | logcat audit lines 13:09–14:09 |
| Export tap SIGSEGV (2×: 13:36 pid 28211, 13:44 pid 28280) | ROOT-CAUSED + FIXED (`ed37ea9`): `exportBackup()` closed the app-wide DB mid-app; the background crawl-heal racer hit the freed statement → native SIGSEGV in libexpo-sqlite (`sqlite3_column_name` use-after-free). Fix: encrypted export ATTACHes the passphrase-keyed output to the LIVE connection (`sqlcipher_export` in place) — no closeDatabase on the path | crash buffer backtraces; post-fix export ran with heal alive, app survived (pid 28941), 7.38MB file written 14:16 |
| Clean-room import spend integrity | FAIL FOUND + FIXED (`2ac16f0`): first gate import showed Home $0.00 / audit 280.95 — import restored only `subscriptions`, and the export strip list (borrowed from cloud-sync scope) deleted `mail_messages`, the classified corpus that IS the sparse spend ($74.84). Fix: encrypted export keeps `mail_messages`; import restores `mail_messages` + `merchant_day_actuals` (0-row no-op for cloud-sync payloads) | audit `projection=280.95 legacy=280.95` 14:23 vs `355.79` pre-export 14:09 |
| **O3 clean-room gate re-run (fixed code)** | **PASS on the user-visible path**: pm clear → fresh install → import gate.cbak (passphrase `gate-phrase-2026`) → "53 subscriptions imported" + Home **$355.79** | /tmp/gate_home.png; audit honestly splits `projection=355.79 legacy=280.95 match=NO` — the legacy fold is low only because gate.cbak was produced by the PRE-fix export that stripped the corpus; the next export (post-`2ac16f0`) carries it full-fidelity |
| Mailboxes after clean-room import | BY DESIGN absent: `mail_mailboxes` stripped (OAuth tokens install-local, dead cross-install); Home shows the reconnect CTA | /tmp/gate_home.png |
| Post-fix export smoke (buckets-only corpus) | PASS — new export path (corpus-keep code) ran clean on device, 192KB file, opened offline with the passphrase: subs=53, buckets=236, mail_messages=0 (corpus genuinely empty on this install; schema-vs-rows note: the strip list DELETEs rows, so only a NON-empty corpus discriminates the fix) | /tmp/gate2.cbak, `/tmp/sqlcv` sqlcipher3-binary |
| OAuth 404 flash | COSMETIC (downgraded from open bug): after Google consent the router briefly shows "Unmatched Route", but the token exchange COMPLETES — next audit shows the mailbox connected with a fresh token; "Go back" recovers to the gate | logcat 17:37–17:41, fetcherFor + "token fresh — using as-is" |
| Restage-on-mailbox-connect | NEW BEHAVIOR RECORDED: connecting a mailbox wipes the imported `merchant_day_actuals` (derived cache) and recomputes from the CURRENT corpus — Home dropped 355.79→280.95 (subs-only) until the rescan rebuilt the corpus. Buckets are never the SSOT; corpus+subs are | audit `buckets=0 ... total=280.95` then rescan |
| Phase 2: reconnect + rescan | PASS — Gmail (david@bohbotweb.com) reconnected, 500 listed / 400 staged, corpus rebuilt; audit `projection=355.79 legacy=355.79 match=yes`; Home $355.79 (screenshot). Workspace mailbox deliberately NOT reconnected: contributes $0 to spend and carries the known 180s stall (separate issue) | /tmp/phase2_home_355.png |
| Phase 3: FIRST full-fidelity export + clean-room restore | **PASS — O3 gate closed with no asterisk**: export of the fixed code with a live corpus = 18.8MB; offline inspection proves `mail_messages` 400 rows IN the file (the discriminating check for `2ac16f0`'s export half) + buckets 236 + subs 53; pm clear → fresh install → import → audit `projection=355.79 legacy=355.79 delta=0.00 match=yes` + Home $355.79 | /tmp/gate3_full.cbak (kept as the only good backup), /tmp/gate3_final_home.png |
| Device cleanup | DONE — /sdcard/Download (gate*.cbak, roundtrip.cbak) and app cache/exports/ emptied | adb listings |
| Housekeeping commits | DONE — user-ordered: drain-inject pin adopted (`8bceea6`), stale 01-operations copy + stray `=` removed (`ef9a5a9`) | git log |
| O1/O2 close-out | DONE — both VERIFIED on device, receipts in hand (`4cab98e`) | /tmp/gate3_final_home.png, /tmp/o2_picker_brands.png |
| **Workspace "180s stall" hunt** | **ROOT-CAUSED from captured evidence — NOT a hang escaping the net; NOT REPRODUCED on current code**: the "180s stall" is the R14 no-progress watchdog (`NO_PROGRESS_BUDGET_MS = 180_000`, scanWatchdog.ts) legitimately failing a silent leg. The capture caught the pre-wipe smoking gun: 13:11:33 `armed 180000ms` → 13:14:33 `no scan progress for 180000ms — failing the leg` (pid 23833, exactly 180s). The underlying silence is the 2026-09-05 wedge class (mid-scan network transients), environmental. Today's full scan on current code: workspace leg armed 19:37:24, ran 8.5 min, **completed 400 staged / 61 candidates, watchdog never fired**, audits `match=yes` throughout (peak `711.58/711.58` as Workspace mail imported new subs, 53→55). Failure loudness verified in source: per-leg catch logs `leg failed` to logcat + UI pill + errors summary — LESSONS 26's fix direction is landed | /tmp/stall_hunt.log (65k lines), scanWatchdog.ts:23, scanConnected.ts:288-305 |
| OAuth "Unmatched route" flash | FIXED (`d5908d0`): Gmail/Workspace return via the Google reverse-client-ID scheme `com.googleusercontent.apps.<bare>:/oauthredirect` (providers.ts:224) — that path had no expo-router route, so the router 404'd while AuthSession captured the code. `app/oauthredirect.tsx` absorbs it (R6/`app/auth.tsx` pattern). Gated on device: fired the exact deep link via `am start` → landed on Subscriptions, no 404 | /tmp/stall_hunt.log, device dump |
| **Paid-unknown display rule restored** (`2c2505f`) | The early-project rule (schema migration 11 "paid-unknown vs known $0"; `ec13e6e` Tuta ?; R18 AS-BILLED) had drifted: `?` rendered on EMPTY actuals windows (`amount===0 && priceUnknown`). Restored: `?` only anchors to a window containing a proven payment with unreadable amount; empty window → `$0.00`; billed sparse+unreadable renders the cadence ("Monthly ?"/"Yearly ?"); recurring priceUnknown → "Monthly ?"; free stays $0.00. Projection parity via schema **v17** `merchant_day_actuals.unknown_count` (fold buckets paid-unknown with total 0; `count` stays known-only for sparseSecondaryLine parity). jest 404/404, tsc 0. Device: the priceUnknown sparse class (Scotiabank, Systems, Publicstorage, Rotaryengine, Nathanandsons, Bedbathandbeyond, Bitget, Coinbase, Equifax) now renders "Monthly ?" — no "This month ?" pairs remain; Home spend unchanged $711.58 | chargeDisplay.test.ts new cases; device dumps |
| Sparse+cadence minting (Transunion $0.00, Uber $47.25, YouTube $549.99, … all "sparse — Monthly") | OPEN DESIGN DECISION — the rows show the AS-BILLED amount honestly, but "Monthly sparse" as a category is the unrecovered piece: user rule = cadence+known-price merchants are RECURRING with a stacked sparse line (Amazon shape). Options: (a) reclassify at mint (spend math changes to amortized), (b) keep sparse actuals spend + render both amounts stacked, (c) leave as-is. Awaiting user pick | Home/Subscriptions dumps |
