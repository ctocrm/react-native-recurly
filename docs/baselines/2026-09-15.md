# 2026-09-15 — Phase K deep re-list device gate

| Claim | Verdict | Receipt |
| --- | --- | --- |
| Deep re-list walks the ENTIRE history, not 500 ids | PASS (device) | `[MailGmail] listed 2098 pages=21 bodies=1695 of 2098` (workspace, 00:56:49); grep count of `limit 500 reached` across `/tmp/k_gate.log` = **0**; proton leg staged 483 (capped era: 398 from workspace alone) |
| Deep run fits its budget (60 min, between-legs check) | PASS | run 00:15:45 → `scan ended (depth=0)` 00:57:45 = **42 min**; old 10-min budget would have killed the workspace leg mid-walk |
| A brand aged past the 500-window re-imports (zerosupporthosting) | PASS | `[BOOT] home render subs=125` (was 124); `startIconCrawl for zerosupporthosting (sub: 1789448262985)` = new row; Home card **Zerosupporthosting $1.99 Monthly** (`/tmp/k15_top.png`); `STARTING SEARCH for zerosupporthosting` fired 00:57:45 — the instant the scan ended (Gate A order) — and J5 short-circuit skipped the dead host in ~7s |
| Keep decision suppresses the import popup during deep import | PASS | scan ran to completion with no modal blocking; Home rendered throughout (logcat `home render` lines 00:57:43→00:59:49) |
| Subs/spend state sane afterwards | PASS | audits: mid-scan all match=yes; 00:58:52 `match=NO delta=150.00` = documented R26 convoy window (rebuild queued 134s behind drain crawls, `projection rebuilt … in 134230ms` at 00:59:49); self-reconciled 01:00:53 `projection=599.68 legacy=599.68 delta=0.00 match=yes`; Home **$599.68** (`/tmp/k15_top.png`); K gate artifact only, no action |
| Bubble: count-only mode then %-mode | PASS | start: a11y node "Deep re-list progress", center "0" (`/tmp/k09_bubble_early.png`); mid-run after Graph total: **100%** green full ring (`/tmp/k11_state.png`, `/tmp/k10_panel.png`); honest "scanned N" rule held — no fake % before a total existed |
| Deep button disabled during run, re-enabled after | PASS | pale/disabled in `/tmp/k11_state.png` during run; solid orange after end (same screenshot class, post-00:57) |
| Normal scan regression: J3 pill, not bubble | PASS | a11y text `Scanning… leg 1/4`, no "Deep re-list progress" node (`/tmp/k16_pill.png`); scan ended 01:09:36 after ~95s, all legs `0 staged` (warm-cache skip — deep run refreshed all caches), same candidates (124/3/9/2) |
| Sheet maxHeight defect (k06: Cancel clipped) fixed on device | PASS | warn sheet + offer sheet with `maxHeight 85%` + `sheetPadding`; Cancel reachable (`k04`/`k07`/`k08` receipts) — ships in this commit |
| Ops | DONE | temp harness removed (`tmp_tapfind.py`, `tmp_wait_deep.sh`); standing watcher fired via `/tmp/k_done.txt`; `.clinerules/hooks` symlinks swept into `1ca1496` remain — user to decide (left untouched per cross-workspace rule) |
| `.clinerules/hooks` pack-wiring resolution (follow-up to the Ops row above) | CLOSED by pack action — nothing for this repo to commit (user-directed) | The pack (YOLO-AI/Extract-Thinking-From-agent-history) removed its own wiring: all 6 event-hook symlinks (PreToolUse, PostToolUse, TaskCancel, TaskComplete, TaskResume, UserPromptSubmit) were unlinked from this project's `.clinerules/hooks/` (git: 6 unstaged ` D` deletions vs `1ca1496`, **left unstaged** — user: "nothing to commit"), and the pack's `hooks/` dir no longer ships the Task*/UserPromptSubmit scripts (now: `cline_tameness.py`, `confirm.py`, `install_artifact.py`, `pull_rules.py`, `record_skill.py`, PostToolUse, PreToolUse). Active hooks come from the global surface (identical bytes, user-verified; local path intentionally not recorded — not found at common home-dir locations, not guessed). The proposed untrack+gitignore plan was dropped as moot. **Standing rule: never re-create local hook links into the pack repo** |

