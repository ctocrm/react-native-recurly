# 2026-09-15 — Phase K deep re-list device gate

| Claim | Verdict | Receipt |
| --- | --- | --- |
| Deep re-list walks the ENTIRE history, not 500 ids | PASS (device) | `[MailGmail] listed 2098 pages=21 bodies=1695 of 2098` (workspace, 00:56:49); grep count of `limit 500 reached` across `/tmp/k_gate.log` = **0**; proton leg staged 483 (capped era: 398 from workspace alone) |
| Deep run fits its budget (60 min, between-legs check) | PASS | run 00:15:45 → `scan ended (depth=0)` 00:57:45 = **42 min**; old 10-min budget would have killed the workspace leg mid-walk |
| A brand aged past the 500-window re-imports (zerosupporthosting) | PASS | `[BOOT] home render subs=125` (was 124); `startIconCrawl for zerosupporthosting (sub: 1789448262985)` = new row; Home card **Zerosupporthosting $1.99 Monthly** (`/tmp/k15_top.png`); `STARTING SEARCH for zerosupporthosting` fired 00:57:45 — the instant the scan ended (Gate A order) — and J5 short-circuit skipped the dead host in ~7s |
| Keep decision suppresses the import popup during deep import | PASS | scan ran to completion with no modal blocking; Home rendered throughout (logcat `home render` lines 00:57:43→00:59:49) |
| Subs/spend state sane afterwards | PASS | audits: mid-scan all match=yes; 00:58:52 `match=NO delta=150.00` = documented R26 convoy window (rebuild queued 134s behind drain crawls, `projection rebuilt … in 134230ms` at 00:59:49); self-reconciled 01:00:53 `projection=599.68 legacy=599.68 delta=0.00 match=yes`; Home **$599.68** (`/tmp/k15_top.png`); K gate artifact only, no action |
| Bubble: count-only mode then %-mode | PASS | start: a11y node "Deep re-list progress", center "0" (`/tmp/k09_bubble_early.png`); mid-run after Graph total: **100%** green full ring (`/tmp/k11_state.png`, `/tmp/k10_panel.png`); honest "scanned N" rule held — no fake % before a total existed |
| Deep button disabled during run, re-enabled after | PASS | pale/disabled in `/tmp/k11_state.png` during run; solid orange after end (same screenshot class, post-00:57) |
| Normal scan regression: J3 pill, not bubble | PASS | a11y text `Scanning… leg 1/4`, no "Deep re-list progress" node (`/tmp/k16_pill.png`); scan ended 01:09:36 after ~95s, all legs `0 staged` (warm-cache skip — deep run refreshed all caches), same candidates (124/3/9/2) |
| Sheet maxHeight defect (k06: Cancel clipped) fixed on device | PASS | warn sheet + offer sheet with `maxHeight 85%` + `sheetPadding`; Cancel reachable (`k04`/`k07`/`k08` receipts) — ships in this commit |
| Ops | DONE | temp harness removed (`tmp_tapfind.py`, `tmp_wait_deep.sh`); standing watcher fired via `/tmp/k_done.txt`; `.clinerules/hooks` symlinks swept into `1ca1496` remain — user to decide (left untouched per cross-workspace rule) |
| `.clinerules/hooks` pack-wiring resolution (follow-up to the Ops row above) | CLOSED by pack action — nothing for this repo to commit (user-directed) | The pack (YOLO-AI/Extract-Thinking-From-agent-history) removed its own wiring: all 6 event-hook symlinks (PreToolUse, PostToolUse, TaskCancel, TaskComplete, TaskResume, UserPromptSubmit) were unlinked from this project's `.clinerules/hooks/` (git: committed as `9adaaae` at user direction after the pack unlinked them), and the pack's `hooks/` dir no longer ships the Task*/UserPromptSubmit scripts (now: `cline_tameness.py`, `confirm.py`, `install_artifact.py`, `pull_rules.py`, `record_skill.py`, PostToolUse, PreToolUse). Active hooks come from the global surface (identical bytes, user-verified; local path intentionally not recorded — not found at common home-dir locations, not guessed). The proposed untrack+gitignore plan was dropped as moot. **Standing rule: never re-create local hook links into the pack repo** |

# 2026-09-15 — Phase M triage-honesty device gate (code `268d743`, PARSER_VERSION 14)

| Claim | Verdict | Receipt |
| --- | --- | --- |
| Freemail hosts never mint the merchant from the host alone | PASS (unit) | 9 new classifier tests incl. "never mints a merchant from a bare freemail host" (`drop:freemail-no-identity`); jest **346/346** (41 suites), tsc 0, eslint 0 |
| Forwarded bill re-keys to the original issuer's From-host | PASS (unit, fixture) | `FIXTURE_FRIEND_FORWARD` (friend@gmail.com → inner `billing@linode.com`): `forward:linode.com`, merchant linode, officialDomain linode.com, sparse |
| google.com-family resolves to the right product, never "Gmail" | PASS (unit) | Workspace/GSuite → workspace.google.com; Cloud/GCP; Ads; One; Play; Drive; YouTube (host-mint kept); generic → "Google"/google.com |
| IDE-crash recovery: emulator reopened | DONE | `adb devices` empty post-crash → `pixel_6a_API34` relaunched cold (`/tmp/m_emulator.log`); Metro already up; `[BOOT] home render subs=125` invariant held |
| Phantom row found and identified (user report made concrete) | PASS (device) | Subscriptions search "gmail" → **Results (1): Gmail, sparse, $250.00 Monthly**; Details: Source `Sean <seantorjman@gmail.com>` · 06/11/2025, subject "Fwd: Order confirmed: MAZDA Genuine RX-8 ENGINE MOUNTS …", bill 100001140 (`/tmp/m02_menu.png`, modal shot after Delete mis-tap) |
| One-time manual delete of the bogus row | PASS (device) | card ••• → Delete → confirm dialog → deleted; search "gmail" → **Results (0)**; spend audit consistent post-delete (449.68/449.68 match=yes) |
| PARSER 14 deep re-list restages everything; phantom does NOT return | PASS (device) | deep 10:45:39 → 11:28; legs tuta 7 / proton 484 / outlook 47 / workspace **1695 staged, 134 candidates** (2233 msgs); post-deep search "gmail" → **Results (0)** |
| The real forward mail now imports triage-honest | PASS (device) | bill 100001140 row = **"Raceroots - Sales — sparse — ?"** (source `raceroots - sales <raceroots@gmail.com>` "Re: Order #100001140"): forward detected → inner sender itself freemail → skipped → display-name sparse, amount honestly "?" — brand-correct, no invented $250, no Gmail row (`/tmp/m05_raceroots.png`-class modal dump) |
| Spend integrity across deep re-imports | PASS | audits `match=yes` throughout; 449.68 → **644.72/644.72 delta=0.00 match=yes** after import (Δ = legitimate aged-out re-imports: 420stock $175, YouTube $1.14, …); Home renders $644.72 (`/tmp/m07_subs.png` era) |
| Warm-cache regression after v14 restage | PASS | follow-up scan legs `0 staged` (tuta leg logged `0 staged, 2 candidate(s)`); audit `match=yes 644.72/644.72` (11:49:46) |
| Subscriptions list clean of Gmail | PASS (visual) | `/tmp/m09_subs.png`: Raceroots - Sales (sparse, ?), YouTube $1.14, 420stock $175 — no Gmail row |
| Ops | DONE | emulator watcher `/tmp/m_deep_done` (self-terminating); logcat capture `/tmp/m_gate.log`; screenshots `/tmp/m01_menu.png`…`/tmp/m09_subs.png`; no temp scripts left in repo |
| Observed, NOT fixed (no scope creep) | RECORDED | collapsed "Scan subscriptions" card shows "No mailboxes yet." placeholder even with 4 mailboxes connected (pre-M quirk, seen 10:36 & 11:56); candidate for Phase O UI polish, not M |


## Phase L session (afternoon) — html recovery root cause

| Claim | Verdict | Receipt |
| --- | --- | --- |
| Root cause: proton/tuta legs never populate message.html (extractor reads only html) | CONFIRMED | imapNative.ts maps only `text` in all 3 normalizers; ProtonModule.kt:491-497 stores decrypted FULL MIME into `text`; TutaModule.kt:290-299 raw HTML into `text` |
| Fix: htmlFromNativeText() recovers html from native text (MIME split + QP/base64 + raw passthrough) | LANDED | commit 579961d; wired into proton/tuta/imap normalizers; PARSER_VERSION 15→16 |
| Unit proof of recovery on real MIME shapes | PASS | htmlFromNativeText.test.ts 6/6 (QP img src=3D, base64, nested multipart, UTF-8 =E2=82=AC, plain-text negative); jest 357/357; tsc clean; eslint clean |
| v16 live restage with fix in bundle | DONE | pid 9820 scan 16:35→16:47: tuta 7 + workspace 398 + outlook 47 + proton 486 = 938 restaged, all legs done, `scan ended (depth=0)` 16:47:16 |
| [EMAIL-DIRECT] / seeds fire from the restaged corpus | FAIL (honest) | zero EMAIL-DIRECT / EMAIL-SEEDS / L-DIAG lines in drain; corpus evidence: workspace 398 + gmail 398 bodies carry NATIVE html (no recovery involved) and still yield zero seeds — real bill emails in these mailboxes embed no absolute-URL logo `<img>` (logos are typically cid: inline, deferred per plan C1 tranche) |
| Phase L visual gate (card icon applied from email) | OPEN | requires ≥1 live seed-bearing brand email in a connected mailbox; pipeline is armed end-to-end (candidate→enqueue→tryApplyEmailIconDirect witnesses at iconBackgroundCrawler.ts:733/757/760) |
| Ops notes | RECORDED | Metro-served bundle verified via entry.bundle curl (L-DIAG present; first curl hit wrong entry `./index` — Expo Router entry is node_modules/expo-router/entry); user_local.db is SQLCipher-encrypted (no direct introspection); v15 restage was consumed before the fix existed → v16 bump required; two rows (Globalindustrial $150.00, Getgreenline) were deleted during flaky blind-UI ops and did not re-stage (aged out of window) — Home spend now 497.72 (audit match=yes), was 644.72 |

## Phase A device gate (first session) — app lock + vault custody

| Claim | Verdict | Receipt |
| --- | --- | --- |
| Foot-down create-pass fires on no-screen-lock device | PASS (visual) | fresh state `none`+insecure → "Create an app password" screen; uiautomator dump `/tmp/pa04.xml`; taps drove enrollment |
| Enrollment wraps key + deletes plaintext, mints BIP-39 phrase | PASS | phrase screen rendered 12 real words (`/tmp/pa06.xml`: parrot…insect); unit vault.test.ts asserts plaintext deletion + blob/meta storage |
| FLAG_SECURE blocks screenshots on phrase screen | PASS | `adb exec-out screencap` returned a **0-byte file** while the phrase screen was up (capture withheld by FLAG_SECURE) |
| Verify-back gates the flow | PASS | wrong-word attempts blocked with error; correct 3 words advanced to unlock |
| Pass mode persists; enter-pass on relaunch | PASS | force-stop → relaunch → "Unlock Cadence" route (`/tmp/pa16.xml`) |
| Wrong pass rejected | PASS | "Wrong password." rendered (`/tmp/pa18.xml`) |
| Correct pass unlocks with DATA INTACT | PASS | unlock → `[BOOT] home mounted`, `home render subs=136`, Monthly Spend **$497.72** preserved through enroll-wrap/unwrap cycle (`/tmp/pa40.xml`) |
| Recovery loop end-to-end | PASS | instrumented: `words=12 → validate=true → unlocked → switching view`; new pass set via setAppPassFromKey; relaunch unlocked with new pass (`/tmp/pa53.xml` Home $497.72) |
| Eye toggle on password inputs (user request) | LANDED | PasswordInput.tsx eye/eye-off; wired into all auth pass fields |
| change-pass/regen/disable device walkthroughs | OPEN | blocked by adb input-injection flakiness (per-char doubling, now fixed via `show_ime_with_hard_keyboard 0` + eye-visible fields); logic unit-proven (changeAppPass round-trip, disable restore) — one clean rerun owed |
| Device-credential (PIN) route on device | OPEN | needs `locksettings set-pin` + app-pass disabled; routing unit-proven (plain+secure → device-prompt) |
| Spend-audit after a real scan post-gate | OPEN | data intact (136 subs, $497.72, audit match=yes pre-gate); mailbox-session scan re-run owed |
| Observed, NOT fixed | RECORDED | WRONG_PASS raw-code leak in AppLockCard fixed in `bf279e4`; Getgreenline/Raceroots rows visible on Home again (re-imported) while spend reads $497.72 — reconcile on next scan audit |

## Phase A device gate (continued) — remaining walkthroughs closed

| Claim | Verdict | Receipt |
| --- | --- | --- |
| Change-pass success on device | PASS | Settings → App Lock → Change (recovery-pass-9 → lock-pass-7) → "App password changed."; relaunch unlocked with the new pass (`/tmp/ga13.xml` Home) |
| Disable app-pass on device | PASS | Disable flow (lock-pass-7) → "App password disabled." + "App password is OFF" + Enable row (`/tmp/ga58.xml`); mode → plain |
| Device-credential (PIN) route | PASS | `locksettings set-pin 1234` → relaunch → system credential prompt auto-fired → PIN → Home (`/tmp/ga60.xml`); emulator PIN left at 1234 |
| Post-gate scan audit | PASS | 4 legs done behind the gate (proton 1 staged, workspace 1), `scan ended (depth=0)` 23:06:42; final `spend-projection-audit: projection=572.56 legacy=572.56 delta=0.00 match=yes` |
| "Empty key" disable error (mid-session) | ROOT-CAUSED, NOT A BUG | double-tap opened a fresh flow and submitted an EMPTY pass; native `SecretKeySpec(empty)` throws "Empty key" — vault state unmangled (ordered writes held); hardening: non-empty validation exists in UI, native rejects now carry stage diagnostics (pwdLen/saltLen/keyLen) |
| JS-reload DB lock (dev-only) | ROOT-CAUSED | JS reload leaks the native SQLite connection; completeGate detects "database is locked" and shows a swipe-away-to-restart message; production never reloads JS |
