# Baseline 2026-09-19 — R38 P2/P3 audit fixes + v30 device gate

> **R40-A addendum (same file, evening):** sort-by-received-date gate PASSED — see rows 9–11 below.

Claims-vs-reality pass at HEAD `2ebd7db` (audit fixes) after the sentinel-audit defects were fixed.

| # | Claim | Verdict | Receipt |
|---|-------|---------|---------|
| 1 | Audit defects A–D fixed (free-flip admission, subject-tier demotion, strong-only stamp clears, $0-tail rollup guard) with tests | VERIFIED | jest 518/518 (62 suites), tsc clean, commit `2ebd7db`; new tests: body-CTA noise stays recurring, welcome mail never flips, generic-only proof never clears stamp, prose tail never mints merchant, $0-tail rollup keeps real charge |
| 2 | PARSER v29→v30 restage converges | VERIFIED | logcat 09-19: `classified load: pages=2 rows=1699`, `spend-projection-audit: projection=189.83 legacy=189.83 delta=0.00 match=yes` |
| 3 | Six-row gate (six-row table) at v30 | VERIFIED | Rows read on device via search + inline Details (uiautomator XMLs in ~/cadence_checkpoints/gate_v30_*.xml): Hightimes recurring $59.99 Monthly Started 09/16/2026; Rockauto sparse ? Started 01/25/2026; Squarespace recurring $41.00 Monthly Started 12/29/2024; Netgate **free** $0.00 Started 02/02/2026 (P3 $0-license flip fired once the deep corpus restored the license mail; Monthly label is the legacy stamp, contributes $0); Rotaryengine sparse $0.00 Started 01/10/2025 (stamp stays cleared); Google Play family card recurring $68.97 **Yearly** + $67.96 Sparse Started 11/25/2022 |
| 4 | Guard: no twin re-mints | VERIFIED | brand searches hightimes/rockauto/squarespace/netgate/rotary/play each returned Results (1) |
| 5 | Guard: subs count moves only by intended changes | VERIFIED with note | subs 104→106: the two extra rows are honest mints from deep-restored mail (e.g. Google One $149.99 Yearly), a Phase-K effect of the v30 corpus rebuild — NOT P2/P3 flips |
| 6 | Guard: spend audit match=yes | VERIFIED | see claim 2; recurring=114.99 sparse=74.84 |
| 7 | Emulator revival without destructive surgery | VERIFIED | cold boot `-no-snapshot-load` from surviving userdata; ANR dialog handled with Wait; lock screen unlocked; mailbox auth persisted |
| 8 | 'play'→Hightimes search-layer bug (unfiled) | NOT REPRODUCED at v30 | search "play" → Results (1) Google Play only |

## Incident during gate (recovered)
The v30 bump wiped the classified corpus (documented bump behavior) and the first restage re-derived a shallow-window world (209.13, recurring inflated, lapsed evidence gone). Recovered via Settings → Deep Re-list (Phase K): walk exhausted at page 22 (+2 ids), 2102 ids listed, deep corpus rebuilt to 1699 rows, audit converged match=yes. The cancelled "Clear Spider / Crawl History" sheet was NOT confirmed — crawl history intact (296).

## Environment note
Late in the session the screenshot-read tool path degraded (PNG and JPEG reads returned "media omitted"); row details were verified via `uiautomator dump` XML instead — same mechanism as the stored v29 Squarespace receipt.

## R40-A device gate (evening, commit `781a2c7` + JS reload via running Metro)
| # | Claim | Verdict | Receipt |
|---|-------|---------|---------|
| 9 | List orders by received evidence, not scan order | VERIFIED | Same Active view: Recent shows Wikimedia/Masaisrael/Malwarebytes top; Oldest flips to Artofliving/Wikimedia/Google-Play(2022)-region top; Akdesigner Details Started 05/06/2017 sits in the old region. Under the old `created_at DESC` the order could not change between sorts — it did. |
| 10 | Five-sort spec + persisted sort + persisted default view Active | VERIFIED | Chips Recent/Oldest/Sparse/Recurring/Next charge all visible (screenshot, chips selected state read visually); Recent selected on first run; after `am force-stop` + relaunch + app-pass unlock, **Oldest + Active restored** (screenshot crop `/tmp/persist_chips.jpg`). |
| 11 | Upcoming un-emptied mechanism, spend unregressed | VERIFIED with note | `nextExpectedChargeFor` projects from row evidence (renewalDate else start+k×cadence; unit-tested 13 cases); Upcoming (0) is CORRECT for this corpus — no row projects a charge within 7d of 09-19 (nearest: Hightimes 10/16, Google Play 11/25). Home Monthly Spend unchanged $189.83; Google Play card still $68.97 Yearly + $67.96 Sparse. |
| — | App-pass gate (A3) observed live | NOTE | The build ships an "Unlock Cadence" app-password screen (vault pass `r38gate1` unlocked 3× across relaunches) — A3 exists in the build; the R40 ledger's A3/A4 item needs a status re-check, not a from-scratch build. |

**Environment notes (R40-A):** dev-client debug build + running Metro (`expo start`, port 8081, adb reverse pre-set) carried the JS-only change with no rebuild; app password `r38gate1` re-required on each fresh JS session. `last_received_at` is NULL on stored rows until the next scan writes it (mint/repair/bookkeeping at scan time) — current on-device ordering falls back to `start_date`, which is itself received-date evidence (P0.5 heal); the R40-D restage populates the column corpus-wide.

