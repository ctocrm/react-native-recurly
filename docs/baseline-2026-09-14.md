# Baseline — 2026-09-14 (claims-vs-reality pass)

**Order:** full baseline of every claim; past 48h treated as untrusted.
HEAD `bce7244` + R13 in the working tree (uncommitted, behavior-neutral
logging — live-proven during this run, D-5). Emulator `pixel_6a_API34`,
logcat capture → `/tmp/scan_logcat.log`.

## A. Static gates — ALL PASS

| Gate | Result | Witness |
|---|---|---|
| tsc --noEmit | PASS, exit 0 | re-executed this session |
| jest | PASS 34 suites, 292/292, 11.6s | 286 committed + 6 in-tree R13 tests |
| Audit §2 witnesses (16 checks) | ALL PASS | ProtonModule :288-289/:822/:845/:859; picker :381; hybrid .25/.12/.35; upscale flag false; SCHEMA 15 + 15 bodies (cosmetic: `// 14:` header missing); PARSER_VERSION 12; kill-switch; 2064 records; 70 tflite; LESSONS 36/37; ProtonModule copies identical; 7199efb=41+/27−; 6f860c8=6+/2− prev.map reportedType |

## B. Device gates

### PASS
- **D-1 Cold boot <5s**: fonts 06:49:38.676 → home render 06:49:40.855 = **2.18s**.
- **D-2 R17 token refresh live ×2**: Proton 06:55:16-17; workspace margin refresh 06:56:11-13.
- **D-3 Incremental cursors**: Proton staged 4 (not 461); Tuta 0; Outlook 47 staged / 3 candidates (Graph listed 323).
- **D-4 Gmail pacing, zero quota walls**: 2000 ids listed over ~40 min at ~1s/msg, no 429s.
- **D-5 R13 progress lines live, format-exact, all 4 legs** (in-tree code).
- **D-6 Projections rebuild live**: 5→53→581 buckets; `delta=0.00 match=yes`; monthly total grew 438.55→599.68 from new workspace actuals.
- **D-7 Insights internally consistent**: 177.08 + 261.47 = 438.55 pre-scan; consistent after.
- **D-8 Settings sync-honesty copy verbatim**; 5 cloud providers.
- **D-9 Picker opens via documented long-press** (swipe X Y X Y 900).
- **D-10 Subscriptions list renders the full table** (CORRECTED — see R-1): scroll reveals xAI, Proton $29.98, Linode $17.00, Zoom, Wolt £16.90, Google, Linkedin, Microsoft, Openai…; `spend-audit subs=125` matches the rendered source; search/chips present.
- **D-11 Zoho self-heal icon brand-correct**: `[HEAL] re-crawling z` → real Zoho tile.
- **D-12 Full scan completed**: "Scan finished. No new subscriptions."; workspace `leg done: 1695 staged, 124 candidate(s)` 07:48:08 — dedupe correct, nothing new to import.

### FAIL (defects found)
- **F-2 On-demand deep search: 0 candidates for `zohoaccounts`** — "Discovered 0 candidates across all sources"; final "1 valid icons saved; 6 retryable". Slug→official-domain mapping gap.
- **F-3 Picker modal not dismissible via X or BACK** — 3 precise X taps + keyevent 4 ignored on the zoho picker; Use Default Icon closes it. (Test side effect: Zoho on default icon; recoverable from 1 saved tile.)
- **F-4 Workspace leg 13-min silent stall; R14 watchdog did not fire.** Progress stopped 07:34:00 (page 20, 50/100 screening); silent until 07:46:36; recovered, finished 07:48:08. No stall/error line. Also: `INITIAL_SCAN_LIMIT=500` (types.ts:108, scan.ts:203) not enforced by the Gmail lister (2000+ listed). Risk: a longer stall would hold `scanState` active indefinitely.
- **F-5 (major) Silent JS-thread hang after scan completion.** Last JS line 07:48:08; by 10:03 taps dead, zero ReactNativeJS output, render loop stalled (7 frame-stats in recent tail vs 3585 total); no ANR/crash/OOM; forced restart required (10:19, new pid 4572, healthy). Likely related to F-4's wedged socket/promise chain. User-facing: app freezes after a scan with no error.

### RETRACTED (tester error — recorded for honesty)
- **R-1 "List renders 2 of 126 rows" — WRONG.** Cause chain: `input tap` injections were eaten (also after restart; `motionevent DOWN/UP` works), so every scroll attempt landed on HOME; the 2 visible rows were viewport, not truncation; scrolling the actual tab reveals the full list. "Insights 125 vs list 2" and "home total vs visible rows" comparisons were the same error.
- **R-2 "Spotify missing from list" — WRONG.** `[CRAWL] startIconCrawl for spotify (sub: none)` + `BUTTON Search pressed for spotify`: the manual add never inserted (tester's form automation fumble — double Create/ESC chaos), and the observed crawl was the picker search button firing with no subscription attached. List behavior correct.

### BLOCKED (unblock: F-4/F-5 first, then F-3)
- Reveal-toggle live cycle (protocol: LESSONS 37) and candidate-grid brand check (G1 standard) — picker reachable, but the cycle needs reliable dismissal (F-3) and a non-hung session (F-5).

## C. Environment notes
- `input tap` eaten after heavy scan session; `input motionevent DOWN/UP` reliable (matches 09-13 injection-mechanics notes).
- JS thread drops touches during heavy staging; two-dump stability mandatory.
- Artifacts: `/tmp/base_01..09_*.png`, `/tmp/ui_*.xml`, `/tmp/scan_logcat.log`.

## D. Repair order (one per session, full loop each)
1. **F-5 + F-4** hang + stall + watchdog no-fire + limit enforcement + scanState risk (same subsystem: scan lifecycle teardown).
2. **F-3** picker dismissal.
3. **F-2** slug→domain mapping for on-demand search.
4. BLOCKED gates (reveal cycle, brand grid), then R13 formal commit (live-proven D-5).
