# Baseline — 2026-09-14 (claims-vs-reality pass)

**Order:** full baseline of every claim; past 48h treated as untrusted.
HEAD `bce7244` + R13 in the working tree (uncommitted, behavior-neutral
logging — live-proven during this run, D-5). Emulator `pixel_6a_API34`,
app pid 2361, logcat capture → `/tmp/scan_logcat.log`.

## A. Static gates — ALL PASS

| Gate | Result | Witness |
|---|---|---|
| tsc --noEmit | PASS, exit 0 | re-executed this session |
| jest | PASS 34 suites, 292/292, 11.6s | 286 committed + 6 in-tree R13 tests |
| Audit §2 witnesses (16 checks) | ALL PASS | ProtonModule :288-289/:822/:845/:859; picker :381; hybrid .25/.12/.35; upscale flag false; SCHEMA 15 + 15 migration bodies (cosmetic: `// 14:` header comment missing); PARSER_VERSION 12; kill-switch; 2064 records; 70 tflite; LESSONS 36/37; ProtonModule copies diff-identical; 7199efb=41+/27−; 6f860c8=6+/2− prev.map reportedType |

## B. Device gates

### PASS
- **D-1 Cold boot <5s**: fonts 06:49:38.676 → home render 06:49:40.855 = **2.18s**.
- **D-2 R17 token refresh live ×2**: Proton 06:55:16-17 rejected→refreshed; workspace margin refresh 06:56:11-13.
- **D-3 Incremental cursors**: Proton staged 4 (not historical 461); Tuta 0; Outlook 47 staged / 3 candidates (Graph listed 323).
- **D-4 Gmail pacing, zero quota walls**: 2000 ids listed over ~40 min at ~1s/msg, no 429s.
- **D-5 R13 progress lines live, format-exact, all 4 legs** (in-tree code): `progress: N staged` + `leg done` per leg.
- **D-6 Projections rebuild live**: 5→53 buckets, 6→48 charges; `delta=0.00 match=yes`.
- **D-7 Insights internally consistent**: 177.08 + 261.47 = 438.55.
- **D-8 Settings sync-honesty copy verbatim**; 5 cloud providers.
- **D-9 Picker opens via documented long-press** (swipe X Y X Y 900).
- **D-10 Add-subscription E2E**: Spotify 9.99 created; auto-crawl fired; real Spotify logo files downloaded (svgrepo/seekvectors/logospng, 07:39-07:42).
- **D-11 Zoho self-heal icon brand-correct**: `[HEAL] re-crawling z` → real Zoho tile on list.
- **D-12 Full scan completed**: banner "Scan finished. No new subscriptions."; workspace `leg done: 1695 staged, 124 candidate(s)` 07:48:08.

### FAIL (new defects)
- **F-1 (major) Subscriptions tab renders 2 of 126+ rows.** Only Zerosupporthosting + Zohoaccounts, before AND after the completed scan ("Scan finished" banner + stale 2-row list in the same dump, 10:03). `spend-audit: subs=125`; Home preview renders Microsoft/Openai/Google/Linkedin rows the tab lacks; new Spotify row never renders. Root cause not yet diagnosed (list query/filter/pagination — R23 keyset class suspect).
- **F-2 On-demand deep search: 0 candidates for `zohoaccounts`** — "Discovered 0 candidates across all sources"; final "1 valid icons saved; 6 retryable". Slug→official-domain mapping gap.
- **F-3 Picker modal not dismissible via X or BACK** — 3 precise X taps + keyevent 4 ignored; Use Default Icon closes it. (Test side effect: Zoho on default icon; recoverable from 1 saved tile.)
- **F-4 Workspace leg 13-min silent stall; R14 watchdog did not fire.** Progress stopped 07:34:00 (page 20, 50/100 screening); silent until 07:46:36; recovered and finished 07:48:08. No stall/error line at any point. Also: `INITIAL_SCAN_LIMIT=500` (types.ts:108, passed at scan.ts:203) not enforced by the Gmail lister (2000+ listed). Risk class: a leg that stalls LONGER may hold `scanState` active indefinitely.

### BLOCKED (by F-1)
- Reveal-toggle live cycle; brand-correct candidate-grid check; Spotify icon visual. Protocol: plan.md 2026-09-13 row.

## C. Environment notes
- JS thread drops touches during heavy staging (tab taps ignored 07:38-07:40) — two-dump stability protocol mandatory.
- Artifacts: `/tmp/base_01..09_*.png`, `/tmp/ui_*.xml`, `/tmp/scan_logcat.log`.

## D. Repair order (one per session, full loop each)
1. **F-1** list-vs-DB (user-facing data-loss class).
2. **F-4** stall + watchdog no-fire + limit enforcement + scanState risk.
3. **F-3** picker dismissal.
4. **F-2** slug→domain mapping.
5. BLOCKED gates, then R13 formal commit (live-proven D-5).
